version: "3.8"

services:
  # 1. Base de Datos (PostgreSQL)
  db:
    image: postgres:15
    container_name: travel_agency_db
    environment:
      POSTGRES_DB: travel_agency_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    networks:
      - backend-network
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # 2. Servidor de Identidad (Keycloak)
  keycloak:
    image: quay.io/keycloak/keycloak:21.1.1
    container_name: keycloak_auth
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://db:5432/travel_agency_db
      KC_DB_USERNAME: user
      KC_DB_PASSWORD: password
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    command: start-dev --import-realm
    volumes:
      - ./keycloak/realm-export.json:/opt/keycloak/data/import/realm.json
    ports:
      - "8080:8080"
    depends_on:
      - db
    networks:
      - backend-network
      - frontend-network

  # 3. Backend (Spring Boot) - Replicado como el ejemplo del prof
  backend1: &backend_base
    build: ./backend # Asumiendo que tienes tu Dockerfile aquí
    environment:
      - DB_HOST=db
      - KEYCLOAK_ISSUER_URI=http://keycloak:8080/realms/TravelAgencyRealm
      # ESTA LÍNEA ES LA CLAVE:
      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=http://localhost:8080/realms/TravelAgencyRealm
      # Y ESTA PARA QUE SEPA A DÓNDE IR FÍSICAMENTE:
      - SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI=http://keycloak:8080/realms/TravelAgencyRealm/protocol/openid-connect/certs
    depends_on:
      - db
      - keycloak
    networks:
      - backend-network

  backend2:
    <<: *backend_base

  # 4. Frontend (React + Vite)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
    - ./frontend:/app # Sincroniza tu carpeta local con /app en Docker [cite: 229, 237]
    - /app/node_modules # Evita que Docker intente sobreescribir los módulos instalados [cite: 237]
    - ./.env:/app/.env
    environment:
      - VITE_KEYCLOAK_URL=http://localhost:8080
    networks:
      - frontend-network

  # 5. Balanceador de Carga (Nginx) - Solo uno para simplificar el inicio
  nginx-backend:
    image: nginx:latest
    ports:
      - "8090:80"
    volumes:
      - ./nginx-backend.conf:/etc/nginx/nginx.conf
    depends_on:
      - backend1
      - backend2
    networks:
      - backend-network

volumes:
  postgres_data:

networks:
  frontend-network:
  backend-network: